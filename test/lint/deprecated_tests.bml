// ==========================================
// BML Category: DEPRECATED - Senior QA Regression Suite
// ==========================================

// 1. Valid Modern alternatives (no warnings)
dJava = strtojavadate("2026-07-03", "yyyy-MM-dd");
nanJava = jNaN;
rsBbmql = bmql("select part_num from parts");

// 2. Deprecated function checks (case-insensitive warnings)
// strtodate -> strtojavadate
dOld1 = strtodate("2026-07-03", "yyyy-MM-dd");
dOld2 = STRTODATE("2026-07-03", "yyyy-MM-dd");

// gettabledata -> bmql
rOldTable1 = gettabledata("parts_catalog", "col", "val");
rOldTable2 = GETTABLEDATA("parts_catalog", "col", "val");

// getpartsdata -> bmql
rOldParts1 = getpartsdata("part_num");
rOldParts2 = GETPARTSDATA("part_num");

// 3. Deprecated NaN constant (case-sensitive warning)
nanOld = NaN;

return string(dJava);

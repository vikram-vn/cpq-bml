items = String[] {"apple", "banana", "cherry"};
for item in items {
	if (item == "banana" OR item == "cherry") {
		print("matched: " + item);
	} elif (item != "apple" AND NOT(item == "pear")) {
		print("other");
	} else {
		print("else block");
	}
}